{
  "name": "build-server",
  "version": "1.0.0",
  "description": "Given a public github repo, it should be able to clone to repo into the container, build it, and store the build output into a S3 bucket.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.569.0",
    "mime-types": "^2.1.35"
  }
}
